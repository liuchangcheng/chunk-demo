{
  "commands": [
    {
      "name": "install",
      "run": "npm ci"
    },
    {
      "name": "test",
      "run": "npm test",
      "role": "gate",
      "timeout": 300
    }
  ],
  "environment": {
    "stack": "javascript",
    "setup": [
      {
        "name": "system",
        "command": "curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash - && sudo apt-get install -y nodejs --no-install-recommends && sudo rm -rf /var/lib/apt/lists/*"
      },
      {
        "name": "install",
        "command": "npm install"
      }
    ],
    "image": "cimg/node",
    "image_version": "22.14.0"
  }
}
